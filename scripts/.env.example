# Staging deployment configuration
# Copy this file to scripts/.env and fill in your values.

# SSH host alias or user@host for the target server
DEPLOY_SERVER=user@your-server

# Path to the docker compose directory on the server
DEPLOY_COMPOSE_DIR=/path/to/compose/directory
