# WebCatalog Server Environment Variables

# Path to WinCatalog SQLite database file
# Required in production, defaults to /data/My WinCatalog File.w3cat
DB_PATH="/path/to/your/My WinCatalog File.w3cat"

# Server port (default: 3000)
PORT=3000

# Node environment (development, production, test)
NODE_ENV=development

# Exclude files or directories from search results
EXCLUDE_PATTERNS=*.tmp,Thumbs.db,.DS_Store

# Minimum file size to include in search results (default: no filtering)
# Format: number + unit (b, kb, mb, gb), case insensitive. Example: 100kb, 5MB, 1gb
# Files smaller than this are excluded from results but still counted in folder sizes
MIN_FILE_SIZE=10B
