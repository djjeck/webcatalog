# WebCatalog Server Environment Variables

# Path to WinCatalog SQLite database file
# Required in production, defaults to /data/catalog.w3cat
DB_PATH=/path/to/your/catalog.w3cat

# Server port (default: 3000)
PORT=3000

# Hour (0-23) for nightly database refresh (default: 0 = midnight)
NIGHTLY_REFRESH_HOUR=0

# Node environment (development, production, test)
NODE_ENV=development
